import { populateContextFieldset } from "./Controllers/contextCheckboxCtrl.js";

function promptCheckboxPopulate(contactKey) {
    populateContextFieldset(contactKey);
}

export { promptCheckboxPopulate};